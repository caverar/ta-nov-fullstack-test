version: "2"
sql:
  - engine: "postgresql"
    queries: "./internal/features/raw-stocks/repository/queries.sql"
    schema: "./pkg/db/migrations/**.up.sql"
    gen:
      go:
        package: "raw_stocks_repository"
        out: "./internal/features/raw-stocks/repository"